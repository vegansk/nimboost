## This module provides some useful data types definitions.

type Unit* = tuple[]
  ## The type that has only one instance: ``()``.
  ## It's an alternative to `void` that allows using it as a value.
